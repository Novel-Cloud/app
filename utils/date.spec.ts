import { getGreeting } from "./date";

test("getGreeting test", () => {
  expect(getGreeting(0)).toBe("Oyasuminasai~");
});

test("getGreeting test", () => {
  expect(getGreeting(1)).toBe("Oyasuminasai~");
});

test("getGreeting test", () => {
  expect(getGreeting(2)).toBe("Oyasuminasai~");
});

test("getGreeting test", () => {
  expect(getGreeting(3)).toBe("Oyasuminasai~");
});

test("getGreeting test", () => {
  expect(getGreeting(4)).toBe("Oyasuminasai~");
});

test("getGreeting test", () => {
  expect(getGreeting(5)).toBe("Oyasuminasai~");
});

test("getGreeting test", () => {
  expect(getGreeting(6)).toBe("Ohayogozaimasu!");
});

test("getGreeting test", () => {
  expect(getGreeting(7)).toBe("Ohayogozaimasu!");
});

test("getGreeting test", () => {
  expect(getGreeting(8)).toBe("Ohayogozaimasu!");
});

test("getGreeting test", () => {
  expect(getGreeting(9)).toBe("Ohayogozaimasu!");
});

test("getGreeting test", () => {
  expect(getGreeting(10)).toBe("Ohayogozaimasu!");
});

test("getGreeting test", () => {
  expect(getGreeting(11)).toBe("Ohayogozaimasu!");
});

test("getGreeting test", () => {
  expect(getGreeting(12)).toBe("Konnichiwa~");
});

test("getGreeting test", () => {
  expect(getGreeting(13)).toBe("Konnichiwa~");
});

test("getGreeting test", () => {
  expect(getGreeting(14)).toBe("Konnichiwa~");
});

test("getGreeting test", () => {
  expect(getGreeting(15)).toBe("Konnichiwa~");
});

test("getGreeting test", () => {
  expect(getGreeting(16)).toBe("Konnichiwa~");
});

test("getGreeting test", () => {
  expect(getGreeting(17)).toBe("Konnichiwa~");
});

test("getGreeting test", () => {
  expect(getGreeting(18)).toBe("Konbanwa~");
});

test("getGreeting test", () => {
  expect(getGreeting(19)).toBe("Konbanwa~");
});

test("getGreeting test", () => {
  expect(getGreeting(20)).toBe("Konbanwa~");
});

test("getGreeting test", () => {
  expect(getGreeting(21)).toBe("Konbanwa~");
});

test("getGreeting test", () => {
  expect(getGreeting(22)).toBe("Konbanwa~");
});

test("getGreeting test", () => {
  expect(getGreeting(23)).toBe("Konbanwa~");
});
